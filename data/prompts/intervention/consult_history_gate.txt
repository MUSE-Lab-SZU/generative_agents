你正在判断当前医患对话是否需要检索“历史医患对话记忆库”。

当前发言者：${speaker}
对方：${other}
pair_key：${pair_key}

当前对话记录：
"""
${conversation}
"""

对方最近一句发言：
"""
${latest_utterance}
"""

任务：
1. 只有在这句发言明确依赖“之前的医患对话内容”才能判定需要检索，例如明确提到“上次/之前/后来/当时/你说过/治疗方案/作业/记录/复盘”等连续性信息，或当前表达若不结合既往医患对话就难以理解。
2. 不要因为主题相似、情绪延续、普通追问、泛泛提到压力/难受/想法，就激进地判定需要检索；如果仅靠当前对话上下文就能回答，应判定为不需要检索。
3. 如果需要检索，请给出一个简洁、适合向量检索的 query；query 要尽量保留核心主题，不要照抄无关修饰语。
4. 如果不需要检索，必须输出 "need_retrieval": false，且 "query": ""。

只输出以下 JSON，不要补充其他内容：
{
  "need_retrieval": true,
  "query": "<query 或空字符串>",
  "reason": "<一句简短原因>"
}
说明：
- need_retrieval 只能输出 JSON 布尔值 true / false，不要输出字符串。
- query 必须输出字符串；当 need_retrieval 为 false 时，query 必须是空字符串。
- 不要输出代码块，不要输出额外解释，不要在 JSON 前后添加任何文字。
