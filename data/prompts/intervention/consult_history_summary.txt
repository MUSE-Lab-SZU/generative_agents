你正在为当前一轮医患对话生成一段“历史对话记忆摘要”，供下一句回复时参考。

要求：
1. 只保留与当前 query 最相关的历史信息。
2. 优先保留：上次在谈什么、患者当时的关键表述/卡点、医生给过的具体建议或作业、哪些回应方式相对有效或无效。
3. 如果多条历史里有重复信息，要合并，不要机械罗列。
4. 不要输出无关历史，不要照抄整段 transcript。
5. 输出要短，适合作为 prompt 中的记忆块；不要写成解释文档。
6. 直接输出摘要正文，不要输出 JSON，不要加标题，不要加代码块。

当前发言者：${speaker}
对方：${other}
pair_key：${pair_key}
当前 query：${query}

当前对话记录：
"""
${conversation}
"""

检索命中的历史记录：
"""
${consult_history_hits}
"""
