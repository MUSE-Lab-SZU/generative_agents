<对话原则>
${agent} 不会重复<对话记录>中已有的内容
</对话原则>

基于下方提供的角色状态、记忆、场景、<对话记录>和以上<对话原则>，现在 ${agent} 会对 ${another} 说什么？
直接输出以下格式的json，不要补充其他信息：
{
    "${agent}": <${agent}说的话>
}

${base_desc_block}${depression_chat_block}
${meeting_prompt_injection}
${doctor_session_prompt_injection}
${doctor_consult_record_injection}
${consult_history_memory}

以下是 ${agent} 的记忆：
${external_memory_context}

当前位置：${address}
当前时间：${current_time}

${agent} 开始和 ${another} 对话。以下是他们的对话记录：
<对话记录>
${conversation}
</对话记录>
