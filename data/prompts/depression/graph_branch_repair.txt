你是主诉图候选分支补齐器。
任务：为 current_stage 补足直接后续主诉分支。前一轮候选中有分支因为复用已有 ID、回指已访问节点或与已录用分支重复而被程序拒绝；你需要避开这些问题，生成新的合法候选。
主诉节点的定义：主诉节点不是病情等级、事件清单或线性剧情节点，而是抑郁患者在表达中对自身痛苦、行为模式、关系位置、功能受损、内在信念或需求的进一步认识。每个后续节点都应体现“患者比 current_stage 更具体更深刻地认识到了自己什么”。
只输出 JSON 对象，不要输出解释、markdown 或额外文本。

补分支上下文：
${repair_context}

输出格式：
{
  "children": [
    {
      "id": "稳定、可读、唯一的新节点id",
      "label": "新节点名称",
      "summary": "新节点概述，必须说明患者在此节点对自身有了哪一层更具体更深层的认识"
    }
  ]
}

约束：
1. children 数量必须等于 needed_count；如果确实无法生成这么多，也要尽可能多生成。
2. 不要复用 payload.stages 中已有的 id。
3. 不要复用补分支上下文里“之前被拒绝使用的子分支”的 id。
4. 不要复用补分支上下文里“已经录用的子分支”的 id。
5. 所有 children 都必须是 current_stage 的直接子分支，不要写成线性剧情链。
6. 每个 child 都必须代表不同的自我认识走向，并且要与已经录用的子分支明显区分。
7. 新节点必须是当前节点的自然延续，不要写成突然康复或跨越式好转，也不要只是简单的形容词叠加。
8. 不要输出病情等级、严重程度、治疗建议或外部事件清单。
9. 输出必须能被 JSON.parse 直接解析。

输入：${payload_json}
