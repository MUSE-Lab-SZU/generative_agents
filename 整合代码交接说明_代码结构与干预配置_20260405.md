# 已整合代码交接说明（代码结构 + 干预配置）

本文用于把当前整合仓库快速交接给新同事，重点说明 `data/config.json` 中 `intervention`、`session_prompt_injection`、`forced_llm`、`depression_dynamic`，以及 `depression_dynamic` 的实际调用链路。

## 1. 代码结构（简版）

当前核心代码在 `generative_agents-2/` 下，建议先看这几层：

- `start.py`：仿真入口，读取 `data/config.json`，创建 `Game` 与 `InterventionManager`。
- `modules/game.py`：初始化所有 agent，并把全局 `intervention.depression_dynamic` 下发到每个 agent 配置。
- `modules/agent.py`：agent 主流程（think/chat/completion），也是动态抑郁提示注入和事件提交的直接调用点。
- `modules/intervention_manager.py`：强制干预总控（规则触发、锁、会话注入、强制 LLM 路由、会后处理）。
- `modules/session_prompt_injection_manager.py`：医生侧 session 注入与 session 进度推进。
- `modules/depression_dynamic_adapter.py`：`depression_dynamic` 的适配层（初始化、提示注入、事件提交、状态持久化）。
- `modules/depression/`：动态抑郁引擎本体（状态机、偏差注入、创伤记忆、prompt 组装）。
- `data/config.json`：全局配置入口（本次重点字段都在这里）。
- `frontend/static/assets/village/agents/*/depression_config.json`：每个 agent 的动态抑郁个体配置。

## 2. `intervention`（总开关与干预域配置）

`intervention` 是干预系统的顶层配置，`InterventionManager` 初始化时会整体读取（`modules/intervention_manager.py`）。

它覆盖的能力包括：

- 医生/患者角色定义、规则调度（`meeting_rules`）与排队（`meeting_queue`）。
- 对话行为控制（`chat_controls`）。
- 会话提示注入（`session_prompt_injection`）。
- 强制 LLM 路由（`forced_llm`）。
- 会诊记录生成/注入（`consult_record`）。
- 抑郁更新模块（`depression_update`，目前你的配置里总开关是 `false`）。
- 动态抑郁适配全局开关（`depression_dynamic`）。
- 记忆策略（`memory_policy`）。

## 3. `session_prompt_injection`（会话提示注入）

### 3.1 字段含义

- `enabled`：是否启用 session 注入。
- `prompt_file`：session 文本来源（JSON）。
- `namespace`：从 `prompt_file` 中取哪个一级命名空间（如 `CBT`）。
- `placeholder`：配置中保留的占位符字符串（当前代码未直接消费该字段）。
- `advance_marker`：医生回复里出现该标记时，判定“本 session 可推进”。
- `order`：session 推进顺序。
- `scope`：语义上是作用域（如 `forced_chat_only`），但当前实现并未显式读取该键做分支。
- `stop_rule_scheduling_on_session_completed`：session 全完成后，停止该 pair 的后续规则调度。

### 3.2 实际注入落点

- 注入内容由 `InterventionManager.get_doctor_session_prompt_injection(...)` 生成（仅强制对话 + 医生发言侧）。
- `agent.py` 在 `generate_chat` 前把该文本作为 `doctor_session_prompt_injection` 传入 prompt 构建。
- 最终落在模板 `data/prompts/generate_chat.txt` 的 `${doctor_session_prompt_injection}` 位置。
- 会话结束后，`SessionPromptInjectionManager.on_chat_finished(...)` 根据 `advance_marker` 推进 `current_session`。

## 4. `forced_llm`（强制路由模型）

### 4.1 字段含义

- 基础连接参数：`enabled / provider / model / base_url / api_key_env / retry / temperature`。
- 路由策略参数：
  - `decide_chat_terminate_retry`
  - `decide_chat_terminate_force_forced_llm`
  - `depr_short_term_retry`
  - `depr_short_term_force_forced_llm`
  - `depr_current_event_retry`
  - `depr_current_event_force_forced_llm`

### 4.2 生效路径

- `agent.completion(...)` 中，如果当前是强制对话并满足 guard（医生-患者配对 + 双方 lock 一致 + API key 可用），会优先走 forced LLM；失败后回退默认 think LLM。
- `InterventionManager.get_forced_llm_runtime_config()` 负责把配置转成可直接建模的 runtime 配置。
- `depr_short_term_*`、`depr_current_event_*` 由 `depression_runtime_manager` 的 `_safe_llm_completion` 读取，用于抑郁更新子流程的路由策略。

## 5. `depression_dynamic`（动态抑郁适配总控）

这是“全局适配层开关”，由 `game.py` 下发到每个 agent（`depression_dynamic_global`），再由 `depression_dynamic_adapter.py` 生效。

字段解释：

- `enabled`：总开关。
- `normal_chain_enabled`：普通链路是否启用（非强制对话）。
- `forced_chain_enabled`：强制链路是否启用。
- `prompt_injection_enabled`：是否向目标 prompt 注入动态层。
- `event_commit_enabled`：是否提交互动事件到动态引擎（更新状态机）。
- `target_hints`：允许注入的函数白名单（如 `generate_chat`、`decide_chat_terminate` 等）。
- `on_missing_agent_config`：找不到 agent 级 `depression_config.json` 时的策略（当前是 `warn_and_disable`）。
- `log_enabled`：是否输出动态适配日志。

补充：动态引擎真正的个体化参数来自 agent 目录下的 `depression_config.json`（如 `depression_simulation.enabled`、`prompt_context_mapping`、`event_interaction_mapping`、`relationship_mapping`、创伤记忆等）。

## 6. `depression_dynamic` 调用链路（重点）

下面是按实际代码执行顺序的链路：

1. 启动读取配置  
`start.py` 读取 `data/config.json` 并组装 `config["intervention"]`。

2. 全局配置下发到 agent  
`modules/game.py` 读取 `config.intervention.depression_dynamic`，拷贝到每个 `agent_config["depression_dynamic_global"]`。

3. agent 初始化动态运行时  
`Agent.__init__` 调 `dda.init_runtime(self, config)`：  
- 读取 `depression_dynamic_global`（全局开关）。  
- 定位 `agent_dir/depression_config.json`。  
- 创建 `DepressionSimulationEngine` 并设置时钟、base prompt。  
- 载入 interaction/relationship 映射。  
- 若 checkpoint 中存在 `depression_dynamic_state`，通过 codec 恢复运行时状态。

4. 对话前提示注入（preview）  
`agent.completion(...)` 在原始 prompt 生成后，先调 `dda.patch_prompt(...)`：  
- 判断是否命中 `target_hints`，以及当前是 normal/forced 链路且对应开关开启。  
- 调引擎 `preview_interaction_prompt(...)` 生成动态心理层文本。  
- 把该层文本前置拼接到原 prompt。

5. 对话后事件提交（commit）  
在两个关键事件点提交：  
- `wait_event`：`_wait_other` 中调用 `dda.commit_event(...)`。  
- `chat_event`：`schedule_chat` 中调用 `dda.commit_event(...)`。  
`commit_event` 内部会调用引擎 `commit_interaction(...)`，推进状态机并记录本次交互影响。

6. 强制链路识别  
`_chat_with(..., forced=True)` 时会设置 `_chat_route_ctx`，`dda.patch_prompt` 通过该上下文判断是否属于 forced chain，从而受 `forced_chain_enabled` 控制。

7. 状态落盘与恢复  
- 落盘：`Agent.to_dict()` 调 `dda.dump_state(...)`，写入 `depression_dynamic_state`（进入 checkpoint JSON）。  
- 恢复：下次启动/续跑时，`dda.init_runtime` 通过 `depression_dynamic_codec.load_state(...)` 回填引擎状态。

## 7. 给接手同事的最小排查路径

如果同事要快速验证该功能是否接通，建议按下面顺序看：

1. `data/config.json` 的 `intervention.depression_dynamic.enabled` 是否为 `true`。  
2. 目标 agent 目录是否存在 `depression_config.json` 且 `depression_simulation.enabled=true`。  
3. 日志里是否出现 `[DEPR_DYNAMIC][INIT] enabled=true`。  
4. 对话阶段是否出现 `[DEPR_DYNAMIC][PROMPT] applied=true`。  
5. 事件阶段是否出现 `[DEPR_DYNAMIC][EVENT] committed=true`。  
6. checkpoint 的 agent 节点里是否有 `depression_dynamic_state`。

