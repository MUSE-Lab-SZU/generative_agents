# 试跑结果

卡布达为患者，使用你的`depression_config.json`配置。跑了48steps，两次都跑完了全部Session。

`sim-dym-severe-kbd-0404`是PST疗法，当前目录同命名文件是`analyze_dynamic_persona.py`分析结果。

`sim-dym-severe-kbd-0404-2`是CBT疗法，当前目录同命名文件是`analyze_dynamic_persona.py`分析结果。

脚本分析结果可以先看`summary.md`再看`depression_state_deep_dive.png`图，两次结果都是更“恶化”了。感觉状态转移那部分需要优化，我看触发词都是与“恶化”有关的？这个我还不太清楚你的代码具体是怎么样触发”恶化“或者”好转“。触发词规则的转移我担心如果患者某句说“我现在虽然{恶化描述}，但是{好转描述}”之类的话，会不会被判定为“恶化”？

PST疗法评估

```
**四、自动化评分提示**
- 结果一致性：**高度一致**。所有五个量表（HAMD: 36分重度，MADRS: 48分极度，BDI-II: 49分重度，PHQ-9: 24分重度，SDS指数: 0.875重度）均指向**重度抑郁**状态，表明患者抑郁症状广泛且严重。
```

CBT疗法评估

```
**四、自动化评分提示**
- 结果一致性：**高度一致**。所有五个量表（HAMD:38分，MADRS:48分，BDI-II:49分，PHQ-9:24分，SDS标准分:88分/指数0.875）均指向**重度抑郁**状态，评估结果相互印证。
```

初始评估（加载第一个json文件）

```
四、自动化评分提示
- 结果一致性：五个量表总分均指向重度抑郁，结果高度一致。HAMD总分35（重度），MADRS总分44（极度），BDI-II总分45（重度），PHQ-9总分23（重度），SDS指数0.85（重度）。
```

# CBT配置和PST配置

在config.json替换字段即可切换

CBT配置

```
"session_prompt_injection": {
    "enabled": true,
    "prompt_file": "data/prompts/intervention_prompts.json",
    "namespace": "CBT",
    "placeholder": "{doctor_session_prompt_injection}",
    "advance_marker": "[SESSION_END]",
    "order": [
        "session1",
        "session_loop",
        "session2.1",
        "session2.2",
        "session2.3",
        "session3.1",
        "session3.2",
        "session3.3-A",
        "session4.1",
        "session4.2",
        "session4.4"
    ],
    "scope": "forced_chat_only",
    "stop_rule_scheduling_on_session_completed": true
}
```

PST配置

```
"session_prompt_injection": {
    "enabled": true,
    "prompt_file": "data/prompts/intervention_prompts.json",
    "namespace": "PST",
    "advance_marker": "[SESSION_END]",
    "order": [
        "session1",
        "sessionloop",
        "session2.1",
        "session2.2",
        "session2.3",
        "session3.1",
        "session3.2",
        "session3.3",
        "session5.1",
        "session5.2",
        "session6.1",
        "session6.2",
        "session6.3"
    ],
    "scope": "forced_chat_only",
    "stop_rule_scheduling_on_session_completed": true
}
```
